# tests:
# - fetching all users from database;
# - adding new user with unique login;
# - adding user with duplicate login returns error;
# - editing existing user data;
# - deleting user;
# - unlocking user (login_attempts to 0);
# - unlocking user with attempts > 0;
