# Entry point of the application
# Initializes the database connection, sets up the main application window, starts the Qt event loop,
# first-run database migrations
# Creates session, controllers, and passes them to views