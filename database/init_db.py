# Database initialization and migration on first run
# Creates all tables defined in models if they don't exist
# Inserts default roles ("admin", "user")
# Called once at application startup from main.py