# Login window widget
# contains: login input field, password input field, login button,
#           and captcha widget (CaptchaWidget)
# handles UI events and delegates authentication logic to AuthController,
# emits signals that are connected to the controller