# Administrator dashboard widget
# displays a table of all users (QTableView) with columns: id, login, role, attempts, full name
# provides buttons: Add User, Edit User, Unlock User, Delete User

# Interacts with UserController for CRUD operations